# === Credentials / Secrets ===
# Context files that contain passwords, API keys, or IBM i credentials
contexts/ibmimcp.md
contexts/inova.md

# Keep self-improvement context (no secrets)
!contexts/self-improvement.md

# === Fleet Runtime State ===
# Workspace is runtime state — regenerated each run, not source of truth
# Using /* (not /) so the heartbeats sub-dir can be un-excluded below
fleet-workspace/*
# Heartbeats are persistent operational signals — tracked in git
!fleet-workspace/heartbeats/
!fleet-workspace/heartbeats/**

# === Node.js ===
node_modules/
dist/
*.js.map

# === Python ===
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# === OS / Editor ===
.DS_Store
.env
.env.*
*.swp
*.swo
.idea/
.vscode/
